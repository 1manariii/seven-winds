import {NavBar} from "./NavBar";
import {Content} from "./Content";

export const Main = () => {
    return (
        <main className="styled-main-wrapper">
            <NavBar />
            <Content />
        </main>
    )
}